# encoding.py
# when we receive a fen string, we need to convert it to a tensor/ bitboard representation
# this is the encoding for the neural network

import numpy as np
import chess

class ChessStateEncoder:
    def __init__(self):
        pass